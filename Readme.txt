VMoodle Report. 

######################################"

this report pulls from all databases the statistic indicators of use of Moodle assets
and global statistics among population and connections.

Note that it WILL NOT WORK correctly for VMoodles which database reside OUTSIDE the main
host DB server, as the same global database connection is used for all queries.